Available:
* Apptainer
* Singularity
* Docker is not available for security reasons.

When you use MPI/cuda inside a container, the version of MPI/cuda inside the container must be compatible with the one on the LEONARDO cluster.